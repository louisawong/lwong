import { Open_Sans } from '@next/font/google'
import { Monoton} from '@next/font/google'

export const monoton = Monoton({weight: '400'})

export const openSans = Open_Sans({weight: '300'})
export const openSans600 = Open_Sans({weight: '600'})